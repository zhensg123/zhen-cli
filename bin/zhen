#!/usr/bin/env node

const program = require("commander");
program
  .version(require("../package").version)
  .usage("<command> [options]")
  .option('--no-sauce', 'Remove sauce')
  .command("vue", "generate a new project from a template");
program.parse(process.argv); // 去掉后不管作用
