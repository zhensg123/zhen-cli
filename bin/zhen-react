#!/usr/bin/env node

const executerTask = require("../lib/entry");
const config = {
  template:'github:zhensg123/cliReactTemplate#master',
  root: '.cliReactTemplate',
  log: "    $ zhen create project-name"
}

executerTask(config)
